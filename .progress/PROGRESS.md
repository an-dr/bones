# Progress

**Flow:** Detailed
**Phase:** COMMIT
**Goal:** Improve game_core_demo (narrower robot hitbox; HUD showing life+score; Esc pause menu with Settings/Exit; Settings recolors big/small boxes) and add the engine capabilities the demo actually needs to support it (extension-triggerable clean exit; a new triangle hazard shape spanning physics+rendering), based on real gaps found while building the demo.
**Done when:** Robot hitbox is visibly narrower than its sprite frame; HUD shows live life and score, score increments on purple-box collision, life decrements on red-triangle collision; Esc opens/closes an in-game menu with Settings and Exit entries; Settings lets the user pick big-box color (warm/non-red presets) and small-box color (cold presets) with changes applying live; Exit cleanly closes the running app; new engine surface (exit request, triangle shape) has tests and updated docs (ADR if it's a lasting architectural call).
**Constraints:** Existing rapier2d/retro dual-world physics and entities stay as-is; former red obstacles become purple ('big boxes'), keep colliding-for-score behavior; blue movers ('small boxes') keep existing behavior; new red triangles are the life-cost hazard; Settings uses preset-color buttons only (no new picker widget) - big-box presets are warm colors excluding red, small-box presets are cold colors; Exit must cleanly close the whole running app, not just the menu - resolved cleanly, not as a minimal stopgap; robot hitbox narrows to ~32px wide (half_w 16).
**Out of scope:** The full roadmap shutdown sequence (per-extension shutdown() lifecycle, Stopped event, close-request pub/sub event) - only a minimal-but-clean extension-exit path; retro-world collider support for triangles unless DESIGN finds it's actually needed (obstacles are RAPIER2D-only today); the old PROGRESS.md's leftover solver-stiffness/velocity-clamp tuning items (unrelated, already-stale prior workflow); a real color-picker widget type (stick to preset buttons).

## Iterations

- [x] **game-core + gfx/renderer + game_core_demo: triangle hazard shape** (completed) — EntityOp::Spawn wire format gains a Shape { Rect, Triangle } field (encode/decode + tests); PhysicsBackend trait's spawn_body takes a Shape instead of only half-extents; Rapier2dBackend builds ColliderBuilder::new(SharedShape::triangle(...)) for Shape::Triangle (isoceles inscribed in half_w/half_h); RetroBackend treats any Shape as its own AABB bounding box (documented limitation - triangles never route to the retro world in this plan); Collider component carries the shape. New gfx::DrawTriangle command (three vertices + color + filled + layer); core/renderer draws it via the existing render_geometry triangle-mesh path (ui_mesh.rs's approach), not fill_rect; game-core's publish_gfx publishes DrawTriangle for Shape::Triangle entities instead of DrawRect. game_core_demo then uses all of this: robot hitbox narrows to half_w 16 (~32px); existing red obstacles recolor to purple ('big boxes', unchanged Dynamic/RAPIER2D behavior, still the score trigger); new red Shape::Triangle hazard entities (stationary, RAPIER2D world) cost life on robot collision; extension-local life/score counters wired off Collision events (purple-box collision -> score++, triangle collision -> life--). Unit tests: a robot driven into a stationary rapier2d triangle stops/collides; existing rect behavior unchanged; a Shape::Triangle entity's published draw command carries triangle vertices matching its collider extent.
- [x] **game_core_demo: HUD** (completed) — Publishes a ui/spec Spec every tick with Label widgets showing current life and score, always visible, positioned so it doesn't overlap the play area.
- [x] **core + game_core_demo: pause menu, settings, exit** (completed) — wit/core.wit gains request-exit: func(); core/wasm-extensions/src/host.rs implements it against a shared Arc<AtomicBool>, threaded through loading.rs/supervisor.rs Host::load call sites; core/runner/src/engine/builder.rs's run loop checks it alongside platform.quit_requested() to break cleanly. game_core_demo then uses it: Esc toggles a menu state (Closed/Main/Settings) in extension state; while open, robot velocity is forced to zero regardless of held input; Main shows Button widgets Settings/Exit; Settings shows preset-color Buttons for big-box (warm, non-red) and small-box (cold) colors, applied live via EntityOp::SetColor to every existing entity of that kind, plus a Back button; Exit calls the new request-exit host-api function. Unit/integration test: calling request-exit from a test extension causes Engine::run to return. Docs: design/platform.md shutdown section gets a short note distinguishing this from the still-open full shutdown sequence.
- [x] **game_core_demo: replace app-UI HUD/menu with a game-styled one** (completed) — Drops bones_messages::ui (Spec/Widget/Clicked) entirely from game_core_demo - no egui window chrome. HUD (score/life) and the pause menu/settings panel are drawn directly via gfx::DrawRect/DrawText (already-precedented pattern, sprite_demo already publishes gfx/* directly from an extension), fixed screen-space pixel positions (camera is always identity in this demo). Buttons are DrawRect+centered DrawText pairs from a single menu_buttons(MenuState) layout list, reused for both drawing and input/mouse-down hit-testing (subscribe MouseDown::TOPIC instead of Clicked::TOPIC) so the clickable area always matches what's drawn. on_button_clicked's menu-transition/recolor/request-exit logic is unchanged, only the input mechanism changes. No engine changes - request-exit stays as already built.
- [x] **game-core + game_core_demo: pause capability** (completed) — EntityOp::SetPaused { paused: bool } (same global-toggle shape as the existing SetDebugHitboxes), skipping physics stepping/contact-clamp/multi-world-resolution/collision-publishing entirely in GameCore::tick while set (still redraws every frame from last-known Transform state, so nothing goes stale/blank). game_core_demo's toggle_menu publishes SetPaused(true) on opening the menu and SetPaused(false) on closing it - the only place menu transitions to/from Closed. Fixes residual box motion (inertia/solver settling) continuing to play out in the background while the pause menu is open, since previously only the robot's own commanded velocity was zeroed, not game-core's own physics stepping for every other entity. Unit tests: a moving/settling entity's Transform does not change across a paused tick; publish_gfx still runs (a Clear/DrawRect/etc. is still published) while paused; unpausing resumes movement.
- [x] **game-core + game_core_demo: real Tiled ground-tile rendering** (completed) — Adds Writer::blob/Reader::read_blob (u32-length-prefixed byte blob, str's counterpart for non-UTF-8/oversized fields) to the shared codec; extends LoadTilemap with tileset_images: Vec<TilesetImage> (name/sprite_id/png_bytes) so a caller can supply tileset art alongside the .tmx. game-core's tiles module gains load_tile_draws (tiled crate, embedded tilesets only) parsing a 'Ground' tile layer into caller-sprite-id-resolved TileDraw records, cached once per load_tilemap call and replayed as gfx::DrawSprite every publish_gfx tick at the new BACKGROUND_LAYER (0, one below ENTITY_LAYER which moves to 1). game_core_demo's level.tmx is expanded with a 64px margin on every side (walls and every entity spawn shifted by the same fixed offset) and its Ground layer painted with real grass/brick tile data instead of the extension hand-computing tile placement - tileset_bricks.png is a new asset cropped from the user's wall tileset (a genuinely tileable brick-coursing region), tileset_grass.png is the user's grass tileset embedded as-is. Also fixes a real bug found live: a background published once at init from the same sender as the per-tick HUD/menu draws was getting wiped out every tick (one retained batch per sender, full replace not a merge) - moving background ownership into game-core (same sender as its own entities) sidesteps this entirely.
- [x] **core/app: resolve bones.toml, extensions_dir, and saves_dir relative to the executable's directory, not the process CWD** (completed) — New core/app/src/paths.rs module: exe_dir() (parent of std::env::current_exe(), None on failure) and relative_to_exe(path) (absolute paths pass through unchanged; relative paths join onto exe_dir() when available, otherwise fall back to today's CWD-relative resolution via an unchanged relative PathBuf). Join logic factored into a pure, env-independent helper so it's unit-testable without mocking current_exe(). core/app/src/config.rs's Config::load signature widens from &str to impl AsRef<Path> (existing str-literal test calls keep compiling unchanged). main.rs resolves the bones.toml path itself via relative_to_exe before Config::load, and resolves config.extensions_dir/config.saves_dir via relative_to_exe before passing them to Engine::extensions_dir/.saves_dir. runner::Engine's own public API and its CWD-relative extensions_dir example in docs/design/modules.md are untouched - scoped to the bones app binary only. Unit tests: the join helper leaves an absolute path untouched; joins a relative path onto a given exe dir; passes a relative path through unchanged when no exe dir is available; Config::load still accepts a bare str path (regression).
- [x] **gfx/renderer + game-core: screen-space draws and camera-follow-with-clamp** (completed) — shared/bones-messages: DrawRect and DrawText each gain a screen_space: bool field (encode/decode + tests) - every EXISTING call site (game-core's publish_shape, sprite_demo, game_core_demo's current 11 DrawRect/DrawText calls) gets screen_space: false added, a pure mechanical no-behavior-change default. core/renderer: RetainedDraw::Text gains screen_space (Rect already wraps the whole DrawRect struct so it flows through for free); inner.rs's record() carries the flag through for Text; composite()'s Rect and Text draw arms skip both the camera translation and zoom scaling when screen_space is true, drawing at literal (x,y,w,h) screen pixels regardless of camera position - Sprite/Line/Circle/Triangle stay world-space only, unchanged, since nothing needs a screen-space version of those yet. bones_messages::game_core::EntityOp gains SetCameraFollow { entity_id: u32, viewport_w: f32, viewport_h: f32 } (TAG 6, encode/decode + tests) - a caller sets this once to have game-core auto-follow that entity. core/game-core: tiles/tile_draw.rs gains map_pixel_size(tmx_bytes) -> Option<(f32, f32)> (a third small reparse alongside load_collision_rects/load_tile_draws, consistent with this module's existing one-parse-per-concern pattern); GameCore stores level_width_px/level_height_px (set in load_tilemap) and camera_follow: Option<CameraFollow{entity_id,viewport_w,viewport_h}>; apply_entity_op handles the new variant; publish_gfx replaces its always-fixed SetCamera{0,0,1.0} with a computed camera when following - centered on the target entity's Transform (self.entities lookup, same pattern as set_velocity/set_color), clamped per axis to [0, max(0, level_size_px - viewport_size_px)] so the camera stops panning once the level edge would go out of view; falls back to the fixed (0,0) camera if not following or the target entity is missing. Unit tests: camera centers on a followed entity mid-map; camera clamps (does not overshoot) near each edge; an unfollowed/default GameCore still publishes the fixed (0,0) camera (regression).
- [x] **game_core_demo: a big grass world with camera-follow** (completed) — level.tmx grows from 38x28 to 120x90 tiles (1920x1440px vs. the 800x600 window - room to actually pan) - Ground layer grass-filled throughout, brick border walls moved out to the new edges; existing obstacles/hazards/boxes stay clustered near the original spawn area rather than being redistributed (the ask is grass coverage + camera, not a level redesign). lib.rs's init calls the new EntityOp::SetCameraFollow { entity_id: 1, viewport_w: 800.0, viewport_h: 600.0 } once, after LoadTilemap so level bounds are already known. Every existing HUD/pause-menu DrawRect/DrawText call (score/life labels, panel backdrop, buttons, button text - added as screen_space: false plumbing in increment 8) flips to screen_space: true, so they stay fixed on screen as the camera pans instead of drifting with the world. Rebuild via build.ps1 and a manual run to confirm grass covers the expanded area, the camera follows and clamps at the level edges, and the HUD/menu stay put.
- [x] **game_core_demo: lower-frequency footstep sound** (completed) — synthesize_tone's footstep call in init drops from 1200Hz/96 cycles (a high-pitched click) to 110Hz/9 cycles (~82ms, a low thud) - amplitude unchanged at 0.5. The hit sound's own doc comment ('a lower blip for a hit, distinct from footsteps') gets corrected too, since footsteps are now the lower of the two tones (110Hz vs the hit's 300Hz), not the higher one. No engine changes - this is entirely inside synthesize_tone's existing caller-supplied parameters.
- [x] **game_core_demo: 30% faster footstep cadence** (completed) — FOOTSTEP_INTERVAL_SECONDS drops from 0.5 to 0.35 (0.5 * 0.7) - the cooldown between footstep sounds while moving, not the robot's movement speed or the tone itself. No engine changes.
- [x] **game_core_demo: footstep cadence tuned further, to 0.2s** (completed) — FOOTSTEP_INTERVAL_SECONDS drops from 0.35 to 0.2 - increment 11's 30% cut still read as too slow once heard; user-directed follow-up tuning to a concrete value.
- [x] **game_core_demo: footstep cadence back to 0.35s** (completed) — FOOTSTEP_INTERVAL_SECONDS drops back from 0.2 to 0.35 - 0.2 read as too fast once actually audible (increment 12's dist/ package had been stale, masking the real effect of both 11 and 12 until this session's rebuild); 0.35 is the value that landed right.
- [x] **game_core_demo: fix non-seamless brick floor tiling** (completed) — level.tmx's brick interior floor picked only 4 of tileset_bricks.png's 16 sub-tiles (local ids 0,3,8,13) via a diagonal dither (col-row) mod 4 - each sub-tile is a real brick-coursing fragment, not an independently tileable swatch, so scrambling their relative position cuts bricks mid-shape at every tile boundary (confirmed by rendering both schemes side by side: the dither produces a broken mosaic, direct reconstruction produces a clean continuous brick wall). This bug originated in the original 38x28 level.tmx (increment 6), just much more visible now across the bigger interior. Fix: the Ground layer's brick placement uses all 16 sub-tiles via direct reconstruction - local_id = (row % 4) * 4 + (col % 4) - which exactly reassembles tileset_bricks.png's real coursing, tiled seamlessly at every boundary. Regenerated level.tmx; the grass margin/wall-band tiles are untouched (already confirmed pixel-uniform, not the source of the seam issue).
- [x] **game_core_demo: interior floor uses rock tiles from tileset_grass, not a separate bricks tileset** (completed) — Replaces the brick interior floor with paving/rock tiles cropped from tileset_grass.png's own cobblestone region (rows 8-9/cols 0-1 and row10 col1 - gids 129,130,145,162), instead of the separate tileset_bricks.png tileset - a genuinely 16px-periodic paving pattern within the source image (grout lines land on the same 16px grid the tile system already slices at), verified seamless under any dither combination by rendering a mixed test grid before use (unlike the bricks source, which was one continuous coursing texture where sub-tile position mattered). Drops the now-unused bricks tileset entirely: level.tmx's <tileset name="bricks"> declaration and tileset_bricks.png asset removed, lib.rs's TILESET_BRICKS_PNG/BRICK_SPRITE_ID/BRICK_TILESET_NAME constants and its TilesetImage registration removed.
- [x] **game_core_demo: rebuild level.tmx at the asset pack's real 32x32 tile size** (completed) — The tileset has been declared/sliced at 16x16 since increment 6, but the source art's real native tile size is 32x32 - corroborated by the robot's own collider (CONTROLLED_HALF_EXTENT=16.0, a 32px-wide hitbox that reads as exactly one 32px tile, not two 16px ones) and confirmed directly by the user. level.tmx's tileset declaration moves to tilewidth/tileheight=32 (grass image is now an 8x8 grid of 64 tiles, not 16x16 of 256); map dimensions recompute to 60x45 tiles to keep the same 1920x1440px world (unchanged, so camera/entity/HUD math stays valid); margin/wall-band tile counts recompute to 2/1 tiles to keep the same 64px/32px pixel thicknesses. Grass margin uses one verified pixel-uniform 32x32 tile (gid 1); the rock-paved interior uses 4 verified-seamless 32x32 crops from the same cobblestone region (gids 33,34,41,50 - each independently grout-bordered, confirmed via a rendered dithered test grid before use, same rigor as increment 15). Regenerates level.tmx; no lib.rs changes needed (entity spawn coordinates are pixel-based, already correct since the world's pixel dimensions are unchanged).
- [x] **gfx/renderer + game-core: display settings (resolution/fullscreen) and camera zoom** (completed) — shared/bones-messages: new gfx::SetDisplay { width: u32, height: u32, fullscreen: bool } command (own file, encode/decode + tests, Command enum variant) - a caller's request to resize/fullscreen-toggle the actual OS window. core/renderer: Inner records the window's initial size at Module::init and calls canvas.set_logical_size(w, h, LetterBox) once (SDL's logical-presentation scaling, sdl3 crate's Canvas::set_logical_size) - every gfx/* coordinate stays in that fixed logical space forever after, so nothing else in the engine (game-core's camera math, an extension's screen_space HUD) ever needs to know or care what the actual window size is. record()'s SetDisplay arm applies immediately (singleton, like Clear/SetCamera): canvas.window_mut().set_size(width, height) then .set_fullscreen(fullscreen) - logical size is never touched, so existing content auto-scales/letterboxes to the new window size with zero ripple effects elsewhere. bones_messages::game_core::EntityOp::SetCameraFollow gains a zoom: f32 field (wire format + existing call-site/test updates - default 1.0 preserves current behavior). core/game-core: CameraFollow stores zoom; camera_position()'s clamp divides viewport_w/viewport_h by zoom before clamping (zooming in shows less world per pixel, shrinking the effective world-space viewport) and publishes the real configured zoom via gfx::SetCamera instead of the hardcoded 1.0. Unit tests: SetDisplay round-trips; SetCameraFollow's zoom round-trips; camera clamp math at zoom != 1.0 (both centers correctly and clamps at a tighter effective viewport).
- [x] **game_core_demo: Display settings section (resolution, fullscreen, zoom presets)** (completed) — Settings panel gains a Display section below the existing Big/Small box color sections: 3 resolution preset buttons (800x600/1024x768/1280x720), a Fullscreen toggle button (label reflects current state), and 3 zoom preset buttons (1.0x/1.5x/2.0x) - same preset-button idiom as the color pickers, no new widget type. PANEL_H grows to fit the extra rows. State gains resolution: (u32,u32), fullscreen: bool, zoom: f32 (defaults 800x600/false/1.0, matching SCREEN_WIDTH/SCREEN_HEIGHT and SetCameraFollow's current init call). on_button_clicked: a resolution preset updates state and publishes gfx::SetDisplay with the new size and the current fullscreen flag; the fullscreen toggle flips state.fullscreen and republishes gfx::SetDisplay with the current resolution; a zoom preset updates state.zoom and republishes EntityOp::SetCameraFollow with CONTROLLED_ENTITY_ID/SCREEN_WIDTH/SCREEN_HEIGHT unchanged and the new zoom - all cheap idempotent re-publishes, no new engine surface. Rebuild via build.ps1 and visually verify each control in the real running app.
- [x] **core: cross-platform display-mode query + a renderer display-changed event** (completed) — New host-api surface (wit/core.wit): record display-mode { width: u32, height: u32 }; list-display-modes: func() -> list<display-mode> (every fullscreen-capable resolution SDL reports for the window's display, genuinely cross-platform via SDL_GetFullscreenDisplayModes); native-display-mode: func() -> option<display-mode> (the desktop's own resolution, SDL_GetDesktopDisplayMode). core/platform: Platform queries and retains this once at construction (before any window hand-off) via self.sdl.video().displays(), exposed as plain accessor methods - a caller has no other way to reach this OS-level info, matching the 'platform is the only component touching the OS' contract. core/wasm-extensions/host.rs: State carries the two query results (computed once, threaded through Host::load the same way exit_requested already is - shared Arc-wrapped data, not a live handle); implements the two new HostApiImports methods. core/runner: builder.rs computes this once from platform and threads it through loading.rs/supervisor.rs's Host::load call sites. Separately: core/renderer's Renderer::new gains a Bus parameter (bypassing ServiceRegistry's single-consumer consume() the same way Ui::new already does, not fighting it) so it can publish a new bones_messages::renderer::DisplayChanged { width, height } event immediately after applying a gfx::SetDisplay command, carrying the REAL resulting window size (queried post-application) rather than what was requested - fullscreen may not honor the requested size, so this is how a caller learns what actually happened instead of guessing. Unit tests: display-mode round-trips through the WIT boundary is covered by existing contract-test patterns where applicable; DisplayChanged encode/decode round-trips.
- [x] **game_core_demo: resolution dropdown + reactive fullscreen-click fix** (completed) — Settings' 3 fixed resolution preset buttons are replaced by a dropdown: a single button showing the current resolution that expands a scrollable-if-needed list of every list-display-modes() entry (deduped/sorted) plus the native-display-mode() one clearly marked, collapsing again on a selection or Back. State subscribes to renderer/display-changed and updates state.resolution from it - fixes the real fullscreen-click bug (mouse hit-testing was guessing the actual window size from what was requested, which fullscreen doesn't honor; now it reflects what the renderer confirms actually happened). No change to the zoom presets or fullscreen toggle from increment 18.
- [x] **game_core_demo: win/lose end screens with Enter-to-restart** (completed) — Life reaching 0 shows a full-screen 'Game Over' overlay; score reaching WIN_SCORE (100, per the bigger arena's more numerous boxes from increment 22) shows 'You Win!\nYou got {score} points!\nPress Enter to restart' - both screen_space, large centered text, blocking further play input. Enter key (a new KeyDown match arm) resets score/life/camera/robot position and re-applies starting entity state, returning to normal play. New MenuState-adjacent end-state enum or a state.game_over: Option<Outcome> field - whichever reads cleaner once the existing menu/input code is in front of me.
- [ ] **game_core_demo: scatter more boxes/hazards across the full interior** (verified) — Expands the handful of big/small boxes and hazard triangles (currently clustered near the original spawn corner) into a scattered layout filling the whole ~1728x1248 interior at a moderate, non-overlapping density - enough to make WIN_SCORE (100) a real target, not clustered near spawn. Existing entity_id constants/arrays (BIG_BOX_IDS, SMALL_BOX_IDS, hazard ids) grow accordingly; spawn_big_box/spawn_small_box/spawn_hazard helpers are reused unchanged, just called with many more coordinates generated programmatically (a grid with jitter/spacing) rather than hand-placed.

_Last updated: 2026-07-23T20:58:30.7362795Z_