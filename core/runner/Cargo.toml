[package]
name = "runner"
version = "0.1.0"
edition = "2021"

[dependencies]
bus = { path = "../bus" }
host = { path = "../host" }
logging = { path = "../logging" }
wasmtime = { version = "46.0.1", features = ["component-model"] }
