# contract

Host-side bindings for the `bones:core` WIT package (`wit/core.wit`),
generated by wasmtime's `bindgen!` macro. No hand-written logic — `core/host`
is the only consumer and never invokes the macro itself.
